const Token = "BOT-TOKEN-GOES-HERE" //The bot's token.
const CommandType = "Both" //Can be Slash, Prefix, or Both. If anything else is put in, Slash will automatically load.

const Prefix = "!" //Prefix command prefix.
